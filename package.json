{
  "name": "webxdc-react-calc",
  "version": "1.0.0",
  "description": "React calculator for Delta Chat",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "pnpx babel --watch src --out-dir . --presets react-app/prod",
    "tailwind": "pnpx tailwindcss -i ./src/input.css -o ./style.css --watch",
    "dev": "concurrently \"pnpm babel\" \"pnpm tailwind\""
  },
  "keywords": [],
  "author": "M4ss1ck",
  "license": "0BSD",
  "dependencies": {
    "babel-cli": "6",
    "babel-preset-react-app": "3"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "tailwindcss": "^3.0.23"
  }
}
